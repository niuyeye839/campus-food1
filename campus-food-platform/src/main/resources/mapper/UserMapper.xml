<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.foodplatform.mapper.UserMapper">

    <select id="selectList" resultType="User">
        SELECT * FROM `user`
        <where>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%',#{keyword},'%') OR email LIKE CONCAT('%',#{keyword},'%') OR student_id LIKE CONCAT('%',#{keyword},'%'))
            </if>
            <if test="status != null">
                AND `status` = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*) FROM `user`
        <where>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%',#{keyword},'%') OR email LIKE CONCAT('%',#{keyword},'%') OR student_id LIKE CONCAT('%',#{keyword},'%'))
            </if>
            <if test="status != null">
                AND `status` = #{status}
            </if>
        </where>
    </select>

</mapper>
